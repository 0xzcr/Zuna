{
  "name": "zuna",
  "version": "0.1.0",
  "private": true,
  "description": "A private, browser-local PDF narration companion.",
  "scripts": {
    "check": "node --check frontend/app.js && node --check frontend/tts-worker.js && node --check frontend/progressive-pages.mjs",
    "test": "node --test frontend/test/*.mjs",
    "dev": "python3 -m http.server 4173 --directory .",
    "preview": "python3 -m http.server 4173 --directory ."
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "onnxruntime-web": "^1.27.0"
  }
}
