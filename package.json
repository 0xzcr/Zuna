{
  "name": "zuna",
  "version": "0.1.0",
  "private": true,
  "description": "A private, browser-local PDF narration companion.",
  "scripts": {
    "check": "python3 -m py_compile backend/dev_server.py backend/kokoro_server.py && node --check frontend/app.js && node --check frontend/progressive-pages.mjs && node --check frontend/reader-core.mjs && node --check frontend/kokoro-runtime.mjs && node --check backend/server.mjs && node --check mobile/src/reader-state.mjs",
    "test": "node --test frontend/test/*.mjs backend/test/*.mjs mobile/test/*.mjs",
    "backend": "node backend/server.mjs",
    "dev": "python3 backend/dev_server.py",
    "preview": "python3 backend/dev_server.py"
  },
  "engines": {
    "node": ">=20"
  }
}
